# In AWS Academy Learner Lab, public bucket policies (Principal: "*"), Public Access
# Block modifications, and Object Lock are blocked by Service Control Policies (SCPs).
# The bucket (var.bucket_name) is created directly in the AWS account, and EC2 instances
# access it securely using their IAM role (LabInstanceProfile).